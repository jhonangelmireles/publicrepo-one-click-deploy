version: 1
frontend:
  phases:
    preBuild:
      commands:
        #- npm install
    build:
      commands:
        #- npm run build
  artifacts:
    baseDirectory: dist
    files:
      #- '**/*'
  cache:
    paths:
      #- node_modules/**/*
backend:
  phases:
    preBuild:
      commands:
        #- echo "This is the AWS CLI version backend prebuild"
        #- aws --version
        #- aws sts get-caller-identity
    build:
      commands: 
        #- '# Execute Amplify CLI with the helper script'
        #- amplifyPush --simple
